input:
  kafka_franz:
    seed_brokers:
      - "redpanda:9092" # Or your Redpanda broker addresses
    topics:
      - "resource_file"
    consumer_group: "metadata_extractor"
pipeline:
  threads: 4
  processors:
  - extract_metadata_processor: {}
output:
  stdout: {}