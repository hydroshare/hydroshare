#!/usr/bin/env bash

# pre-remove-hs
# Author: Michael Stealey <michael.j.stealey@gmail.com>

DIR_PREFIX=$(pwd | rev | cut -d '/' -f 1 | rev | sed 's/[\._-]//g')
docker exec ${DIR_PREFIX}_hydroshare_1 /usr/bin/env bash scripts/remove-root-items

echo "*** FINISHED SCRIPT pre-remove-hs ***"
exit 0;
